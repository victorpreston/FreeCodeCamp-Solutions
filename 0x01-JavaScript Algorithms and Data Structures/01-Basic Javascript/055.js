// Setup
let sum = 0;

function addThree() {
    sum = sum + 3;
}

// Only change code below this line

function addFive(value) {
    sum = sum + 5;
    return value;
}

// Only change code above this line

addThree();
addFive();